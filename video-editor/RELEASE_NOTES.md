# Release Notes

## 0.1.0 — Initial Release

- Published the root repository package for the Video Editor web suite.
- Added a proper root entrypoint at `index.html`.
- Included PWA metadata in `manifest.json` and an offline-capable `sw.js`.
- Added package metadata in `package.json` for repository and local development metadata.
- Added release notes and usage guidance for local hosting.
